language: python
python:
  - "3.5"
  - "3.6"
# command to install dependencies
install:
  - pip install . 
# command to run tests
script:
  - coverage run test/test_vasp_input.py 

after_success:
  - codecov
